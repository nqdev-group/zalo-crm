{
  "name": "zcrm",
  "private": true,
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "db:seed": "turbo run db:seed"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
